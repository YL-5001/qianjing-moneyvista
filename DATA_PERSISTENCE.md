# 数据持久化与更新规则

钱景的目标、账户、储蓄记录、交易记录和策略配置均存储在 Cloudflare D1 的 `DB` 绑定中；它们不是浏览器临时状态。

## 本地开发

- 本地 D1 数据位于 `.wrangler/state/v3/d1/`。它被 Git 忽略，因此拉取代码、构建和常规功能更新不会覆盖它。
- 不要删除 `.wrangler/state/`；如需重置本地演示数据，应先导出或备份该目录。
- 初始化演示数据只会写入完全空的新数据库。即使初始化标记缺失，只要已有目标或账户，应用也不会再次插入演示数据。

## 功能更新

- 所有结构变更都必须先修改 `db/schema.ts`，再运行 `npm run db:generate`，并将生成的 `drizzle/` 迁移文件一同提交。
- 迁移必须向前兼容：优先新增表、可空列或带默认值的列；禁止在没有迁移方案和备份的情况下删除、重建或清空业务表。
- 更新上线前必须检查迁移 SQL，确认其中不包含针对 `goals`、`goal_records`、`asset_accounts`、`asset_transactions` 或 `settings` 的意外 `DROP`、`DELETE`、`TRUNCATE` 或重建操作。

## 已部署数据

- 部署环境继续使用同一个 `.openai/hosting.json` 中的 `DB` 绑定；发布新代码不会创建或替换数据库。
- Cloudflare D1 的 Time Travel 可用于恢复错误迁移或误删前的状态。对重要版本变更，先确认恢复点，并在需要更长保留周期时导出数据库。
